# Video Downloader UI 设计方案

> 版本：0.9（Clash 风格桌面浅色 + Graphite Rose 移动端）
> 更新日期：2026-09-07
> 适用范围：Qt 6 + QML/Qt Quick Controls 2 + Kirigami
> 当前平台：Windows、macOS、Linux、Android（基础验收基线）
> 后续平台：iOS（先完成布局、工具链和权限可行性验证）

当前落地状态：视觉令牌、桌面工作台、移动端单列布局、Signal Trace、共享组件和三类页面已经完成 Qt Quick Controls 2 功能基线。项目已固定并接入 Kirigami 6.8.0（ECM 6.8.0）：桌面端检测到依赖时使用 `MainKirigami.qml`，并进一步使用 `Kirigami.ScrollablePage`、`Kirigami.Card`、`Kirigami.InlineMessage`、`Kirigami.ActionToolBar`、`Kirigami.PlaceholderMessage`、`KirigamiLayouts.FormLayout` 和窄桌面 `Kirigami.NavigationTabBar` 组织页面；宽桌面继续使用 `GlobalDrawer`。Windows 默认使用参考截图启发的浅色下载工作台：中性灰画布、白色内容卡片、蓝色主动作和低饱和选中态，绿色只表示工具可用或任务完成；桌面侧栏收窄为约 196 px，右侧活动栏约 300 px。Windows 优先使用 `Microsoft YaHei UI` 和 `Cascadia Mono`，Android 优先使用 `Noto Sans CJK SC`，其他平台继续使用对应系统字体回退。Android 默认继续使用 Graphite Rose 深色主题。未安装依赖时仍回退到 `Main.qml` 的响应式壳层。Android 构建继续显式关闭 Kirigami，使用已验收的 Qt Quick Controls 2 页面，避免把只在桌面环境提供的插件直接带入 APK。Android 运行时已改用维护中的 FFmpegKit full AAR，并修正了把嵌入式 FFmpegKit 误判为缺失的问题。

跨平台改造的阶段状态和后续任务见：[ReClipQt-跨平台改造计划.md](ReClipQt-跨平台改造计划.md)。

## 1. 产品定位

Video Downloader 是一个面向普通用户的媒体下载工作台。它的核心任务不是展示很多设置，而是让用户快速完成：

```text
粘贴链接 → 识别媒体 → 选择格式 → 开始下载 → 查看结果
```

用户应该始终知道三件事：

1. 当前正在处理哪个媒体。
2. 下载进行到了哪里。
3. 下一步可以做什么。

### 1.1 设计关键词

- 清晰：首屏只强调“粘贴链接”和“开始下载”。
- 稳定：任务状态、失败原因和可执行操作必须明确。
- 有信号感：解析和下载过程像一条可靠的媒体信号链，而不是跳动的装饰动画。
- 跨设备：桌面端适合鼠标、键盘和多任务；移动端适合拇指、单列和页面返回。
- 克制：不使用大面积渐变、无意义的卡片堆叠或装饰性数据。

## 2. 视觉方向：Signal Desk · Desktop Blue / Mobile Graphite Rose

“Signal Desk”把 Video Downloader 设计成一个安静的媒体控制台，并根据使用场景采用两套互相兼容的视觉基线：Windows 桌面端采用参考截图启发的浅色工作台，移动端继续采用 **Graphite Rose** 深色画布。两端都只让一个主色承担主要动作，颜色只承担层级和状态，不用渐变、发光或大量彩色卡片制造存在感。

### 2.0 Windows 桌面色彩与布局方向

Windows 桌面端不再把手机深色主题直接放大到工作台，而采用更适合长时间使用和多任务操作的中性浅色层级，并吸收参考界面的信息密度：

- 画布使用 `#F3F4F6`，内容区使用白色，侧栏使用 `#F0F2F5`，让导航和工作区自然分开。
- 主动作使用蓝色 `#1677FF`，用于“解析媒体”“开始下载”、焦点边框和拖拽提示；绿色 `#07C160` 只表示工具可用、媒体准备好或任务完成。
- 选中态使用浅蓝底 `#DCEBFC`，文字保持深色；悬停只提升一层灰度，不使用强阴影或荧光边框。
- 左侧导航约 196 px，采用紧凑的图标、标题和低对比度选中胶囊；右侧活动栏约 300 px，与主工作区使用同一套卡片令牌。
- 桌面按钮、输入框和面板的圆角收敛到 5–9 px，保留移动端的胶囊状态标签，但不把所有控件做成胶囊。
- 这是对参考界面信息层级的借鉴，不复制 Clash 的图标、代理文案或业务字段；Video Downloader 仍以链接、媒体预览和下载队列为中心。

### 2.1 手机截图参考结论

本轮参考了手机 `DCIM/Screenshots` 中 2026-09-06 21:48 生成的三张截图，分别呈现“仪表盘”“配置”和“工具”三类页面。截图只作为视觉参考，不复制其中的网络代理文案、业务字段或专有图标。

需要吸收的设计语言：

- **深色优先**：使用近黑色全屏画布，状态栏、页面内容和底部导航保持连续，不出现亮色顶栏。
- **单一暖色主动作**：用浅灰粉色表示当前选中项、主要按钮和悬浮操作；同一页面只保留一个最强操作焦点。
- **绿色只表示健康状态**：例如工具可用、检查通过或下载完成，不把绿色当作装饰色。
- **少量容器，明确层级**：仪表盘可以使用少量大面板；配置页使用单个配置卡；工具页改用整行列表和细分隔线，不把每一行都包成卡片。
- **选中态是胶囊而不是高亮整页**：底部导航选中项使用低对比度灰粉胶囊，图标和文字仍保持清晰。
- **图标优先、文字简短**：顶栏只放页面标题和必要动作；列表行使用左侧图标、标题和一行说明，避免堆叠标签。
- **移动端边到边**：保留系统安全区，但页面背景延伸到状态栏和导航手势区域；内容通过留白而不是厚重阴影分层。

本轮截图暂存于 `.scratch/phone-ui-references/`，不作为应用资源提交，也不影响运行时打包。

唯一需要被记住的视觉元素是 **Signal Trace（信号轨迹）**：

- 首次解析时，输入区底部出现一条使用当前平台主色向右推进的细线：Windows 为蓝色，Android 为灰粉 Rose。
- 下载过程中，进度条使用同一条轨迹，不额外添加闪烁、粒子或渐变。
- 完成后轨迹变为青绿色，并在末端显示一个静态完成标记。
- 失败时轨迹停在失败位置，转为珊瑚色，同时显示具体原因和下一步操作。

这样，解析、下载、完成和失败使用同一个视觉语法，用户不需要重新学习状态。Signal Trace 在 Windows 上以蓝色表示进行中、以绿色表示完成，在 Android 深色主题上以 Rose 表示进行中色；失败仍统一为低饱和红色。

## 3. 设计令牌

### 3.1 核心色板

| 令牌 | Hex | 用途 |
| --- | --- | --- |
| `Graphite` | `#151314` | 移动端默认画布、桌面深色备选主题 |
| `Surface` | `#211D1E` | 面板、底部导航和输入容器 |
| `Divider` | `#4B4546` | 卡片边框、列表分隔线和非选中图标 |
| `Text` | `#F1E8E9` | 主文字、页面标题和主要图标 |
| `Rose` | `#E3CDD1` | 主按钮、选中胶囊、悬浮操作和进行中轨迹 |
| `Signal` | `#80C43A` | 成功、完成、工具正常和健康状态 |

Windows 桌面浅色令牌：

| 令牌 | Hex | 用途 |
| --- | --- | --- |
| `Desktop Canvas` | `#F3F4F6` | 窗口背景和页面留白 |
| `Desktop Sidebar` | `#F0F2F5` | 左侧导航背景 |
| `Desktop Surface` | `#FFFFFF` | 输入区、媒体预览和内容面 |
| `Primary Blue` | `#1677FF` | 主动作、焦点边框和拖拽提示 |
| `Primary Blue Soft` | `#E7F1FF` | 图标底、轻量操作面和提示背景 |
| `Signal Green` | `#07C160` | 工具可用、媒体就绪和任务完成 |
| `Selection Surface` | `#DCEBFC` | 当前导航和轻量选中态 |
| `Desktop Divider` | `#E6E8EB` | 分隔线和控件边框 |

派生表面色：

```text
深色 surface-muted #2A2526
深色 surface-raised #5B5354
深色 text-secondary #C1B4B6
警告               #DDB767
错误               #D77979
浅色桌面画布       #F3F4F6 / #FFFFFF / #E6E8EB
```

颜色使用规则：

- 桌面端 `Primary Blue` 每个页面最多有一个主要视觉焦点；移动端 `Rose` 保持同样的单焦点规则。
- `Signal` 不用作装饰，只表示“已经可用”或“已完成”。
- `#D77979` 只用于错误、危险操作和明确的取消状态；错误仍然必须配合文字说明。
- Windows 默认采用桌面浅色主题；Android 默认采用 Graphite Rose 深色主题，用户仍可以在设置中手动切换。
- 不使用渐变、玻璃拟态和大面积阴影；层级主要由 `Surface`、`Divider`、留白和文字对比建立。

### 3.2 字体

优先使用系统字体，确保中英文和 Android 设备上的字形自然。字体由 `qml/Theme.qml` 按平台选择，Qt 负责缺失字形的回退：

- Windows 主字体：`Microsoft YaHei UI`；Windows 技术信息：`Cascadia Mono`。
- macOS 主字体：`PingFang SC`；其他桌面 Linux 主字体：`Noto Sans`。
- Android 主字体：`Noto Sans CJK SC`；Android 技术信息使用系统 `monospace`。
- 技术信息只用于 URL、文件路径、版本号和进度数字；中文正文不使用等宽字体。
- 不使用全大写标签，不使用大面积字母间距；页面标题采用自然的中等字重，不做海报式加粗。

| 语义 | 桌面端 | 移动端 | 字重 |
| --- | ---: | ---: | --- |
| 页面标题 | 24 px | 28 px | 600 |
| 区块标题 | 18 px | 17 px | 600 |
| 正文 | 14 px | 16 px | 400 |
| 按钮 | 14 px | 15 px | 600 |
| 辅助信息 | 12 px | 13 px | 400 |
| 技术信息 | 12 px | 12 px | 400，等宽 |

### 3.3 间距、尺寸和形状

```text
基础间距：4 / 8 / 12 / 16 / 24 / 32 px
桌面内容最大宽度：1180 px
桌面侧边栏：196 px
桌面右侧活动栏：300 px
移动端页面水平边距：16 px
桌面端最小按钮高度：34 px（普通按钮 40 px）
移动端最小触摸区域：44 × 44 dp
输入框圆角：16 px
普通控件圆角：12 px
移动端内容面板圆角：20 px
状态标签圆角：999 px
底部导航选中态：999 px 胶囊
大型页面区块：使用分隔线和留白，不默认套圆角卡片
```

移动端最小水平留白为 16 dp，主要区块之间使用 24 dp；列表行高度建议 80–96 dp，分隔线保持 1 px。阴影只用于浮层、菜单和底部抽屉，普通内容通过背景层级、分隔线和留白区分。

## 4. 信息架构

### 4.1 共享页面

```text
新建下载
├── 链接输入
├── 媒体预览
├── 格式和清晰度选择
└── 开始下载 / 加入队列

下载队列
├── 当前任务
├── 等待中的任务
├── 已完成任务
└── 失败任务

设置
├── 下载目录
├── yt-dlp / FFmpeg
├── 默认输出格式
├── 界面语言
└── 界面主题

工具诊断
├── yt-dlp 状态
├── FFmpeg 状态
└── 重新检测和路径配置
```

### 4.2 导航原则

- 桌面端将“新建下载”“下载队列”“设置”放在左侧导航；“工具诊断”放入设置的子页面，也可以在工具异常时作为明显入口出现。
- 移动端使用底部三项导航：`新建`、`队列`、`设置`。工具诊断作为设置中的页面，不占用底部导航位置。
- 从预览页进入下载详情时使用页面栈，不弹出深层嵌套对话框。
- 选择格式、清晰度等短决策使用底部抽屉或内联展开，不使用多层模态窗口。

## 5. 桌面端设计

### 5.1 桌面端整体布局

适用宽度：`≥ 960 px`。核心目标是同时看见输入、媒体信息和任务活动。

```text
┌────────────────┬─────────────────────────────────────────┬────────────────────┐
│ Video Downloader│ 新建下载                                │ 活动                │
│                │ 粘贴媒体链接，几秒内开始                 │ 当前任务            │
│  + 新建下载    │                                         │ ────────────────── │
│  下载队列      │ ┌─────────────────────────────────────┐ │ 解析中              │
│                │ │ 粘贴一个或多个媒体链接         ⌘↵ │ │ example.mp4         │
│  设置          │ │                                     │ │ 43% · 2.4 MB/s      │
│                │ │ https://…                           │ │ [取消]              │
│                │ └─────────────────────────────────────┘ │                    │
│                │ [解析媒体]             [清空]            │ 已完成              │
│                │                                         │ clip-01.mp4         │
│  工具状态      │ ───── Signal Trace ─────────────────── │ [打开文件]          │
│  ● 已就绪      │ 媒体预览                                │                    │
│                │ ┌──────────────┐  标题                 │                    │
│  v0.1          │ │              │  作者 / 时长          │                    │
│                │ │   缩略图     │  [MP4] [MP3]           │                    │
│                │ └──────────────┘  [开始下载]            │                    │
└────────────────┴─────────────────────────────────────────┴────────────────────┘
```

布局说明：

- 左侧导航固定，宽度不随内容变化。
- 中间工作区是视觉主角，内容最大宽度约 760 px，左对齐，不居中堆叠。
- 右侧活动栏显示当前任务和最近完成任务，不重复完整下载卡片。
- 输入区是页面唯一的大型强调表面，媒体预览和队列使用轻分隔结构。
- 窗口宽度不足 1180 px 时，右侧活动栏折叠为顶部 `活动`按钮或抽屉。

### 5.2 桌面端“新建下载”页面

首屏只保留一个主要动作：解析链接。

#### 输入状态

- 标题：`新建下载`
- 辅助文案：`粘贴链接，先确认媒体信息，再选择输出格式。`
- 多行输入框支持空格、逗号和换行分隔。
- 主按钮：`解析媒体`
- 次要动作：`从剪贴板粘贴`、`清空`
- 拖拽进入窗口时，整个工作区显示半透明蓝色边框，文案为：`松开以解析链接`。

#### 解析成功状态

输入区下方出现 Signal Trace，随后展开媒体预览：

- 缩略图比例 16:9，桌面端宽度约 280 px。
- 标题最多显示两行，作者和时长作为次级信息。
- 输出格式使用两个明确的选择项：`MP4 视频`、`MP3 音频`。
- 清晰度选择显示质量、分辨率和文件大小，不只显示内部 format id。
- 主按钮根据选择变化：`下载 MP4` 或 `下载 MP3`。
- 次要按钮：`加入队列`。

#### 解析失败状态

错误区不使用模糊的“操作失败”。采用：

```text
无法读取这个链接
yt-dlp 没有返回可用的媒体信息。
检查链接是否公开、工具是否可用，然后重试。

[重新解析]  [打开工具诊断]
```

### 5.3 桌面端队列页面

队列使用“任务行 + 状态轨迹”，而不是多个大小相同的卡片：

```text
下载队列                                      [清空已完成]

正在下载                                      1 个任务
┌─ 01 ─────────────────────────────────────────────────────────────┐
│ ● example-video                                                   │
│   MP4 · 1080p · 43%                         2.4 MB/s · 00:18       │
│   ━━━━━━━━━━━━━━━━╸                                             │
│                                  [取消] [打开所在目录]             │
└───────────────────────────────────────────────────────────────────┘

等待中                                        2 个任务
02  another-video              MP3 · 等待中                         [取消]
03  tutorial                   MP4 · 等待中                         [取消]

已完成                                        今天
04  finished-video             MP4 · 182 MB                         [打开文件]
```

状态颜色只出现在任务编号、Signal Trace 和操作按钮上，标题本身保持可读性。

### 5.4 桌面端设置和工具诊断

- 设置页使用两列：左侧设置分类，右侧当前表单；窄窗口时变为页面栈。
- 路径字段使用等宽字体，并支持中间省略。
- 工具状态用一行“状态 + 版本 + 路径 + 操作”，避免三层嵌套卡片。
- `yt-dlp` 和 `FFmpeg` 的状态颜色要同时配合文字：`已就绪`、`未找到`、`版本检测失败`。
- 危险操作“恢复默认设置”放在页面底部，用低强调按钮，不和保存按钮并列。

### 5.5 桌面端对齐移动端参考风格

桌面端不直接复制手机底部导航，而是把同一套信息层级扩展为工作台；色彩采用独立的浅色基线：

- 默认使用 `#F3F4F6` 画布、白色内容面和 `#F0F2F5` 侧栏；侧边栏选中项使用浅蓝底，未选中项保持透明。
- 中央工作区保留输入、媒体预览和队列的任务关系，但面板只使用细描边和留白，不连续堆叠相同圆角卡片。
- 桌面主按钮使用 `#1677FF`，每个页面只保留一个高强调动作；“取消”“恢复默认”等操作使用低强调或危险色。
- 工具诊断、活动栏和状态信息沿用移动端“图标 + 简短标题 + 一行说明”的层级，避免桌面专属的小标签和装饰性统计。
- 当窗口进入窄桌面或平板宽度时，侧边栏折叠为页面标题和返回按钮，底部导航的选中胶囊语义不变。

### 5.6 本轮参考界面的落地项

- `qml/Theme.qml` 增加 Windows、macOS、Linux 和 Android 的字体选择，并集中维护桌面蓝色、成功绿色、背景灰和分隔线令牌。
- `DesktopSidebar`、`NavigationItem`、`AppButton` 和 `ActivityPanel` 调整为参考界面的紧凑导航、白色面板和细边框节奏。
- `LinkCapture`、`MediaPreview` 和三类页面统一使用约 24 px 桌面内容留白、16 px 区块间距和 5–9 px 面板圆角。
- Kirigami 和 Qt Quick Controls 2 两套入口同步使用相同令牌，保证依赖存在与回退场景的视觉方向一致。

## 6. 移动端设计

### 6.1 移动端整体布局

适用宽度：`< 600 px`。移动端不是把桌面端缩小，而是把一个下载任务拆成连续、可返回的步骤。

```text
┌────────────────────────────┐
│ Video Downloader        ⋯    │  顶部应用栏
├────────────────────────────┤
│ 新建下载                    │
│ 粘贴链接，马上开始          │
│                            │
│ ┌────────────────────────┐ │
│ │ 粘贴媒体链接           │ │
│ │                        │ │
│ └────────────────────────┘ │
│ [从剪贴板粘贴]             │
│                            │
│ ┌────────────────────────┐ │
│ │ 解析媒体               │ │  主动作
│ └────────────────────────┘ │
│                            │
│ 最近任务                   │
│ 01  example-video   43%   │
│                            │
├────────────────────────────┤
│  新建          队列    设置 │  底部导航
└────────────────────────────┘
```

移动端规则：

- 页面水平边距 16 px，主要按钮全宽。
- 所有可点击区域至少 44 dp 高。
- 不在首屏同时展示工具路径、清晰度策略等高级设置。
- 预览结果使用全宽缩略图，格式和清晰度通过底部抽屉选择。
- 下载任务用单列列表，左侧状态点 + 标题，右侧进度或动作。
- 任务操作使用左滑操作或详情页，不在一行放置三个小按钮。

### 6.4 手机截图对齐规则

下面的规则优先级高于旧版移动端线框中的颜色和容器样式；功能信息架构不变，只调整视觉表达：

```text
┌────────────────────────────┐
│ 新建下载                 ⋯  │  Graphite 顶栏
│                            │
│ ┌────────────────────────┐ │
│ │ 媒体链接               │ │  Surface + Divider
│ │ https://…              │ │
│ │                        │ │
│ │ [从剪贴板粘贴]         │ │
│ └────────────────────────┘ │
│                            │
│                 [解析媒体] │  Rose 主动作
│                            │
│ 最近任务                   │
│ ────────────────────────── │  细分隔线
│ ● example-video     43%   │
│                            │
├────────────────────────────┤
│  新建          队列      设置 │  选中项为灰粉胶囊
└────────────────────────────┘
```

- **顶栏**：深色背景直接延伸到状态栏，左侧为页面标题，右侧只放当前页面必需的刷新、筛选或更多操作；不额外增加彩色标题栏。
- **仪表盘/新建页**：输入区和当前任务使用 1–2 个大面板；面板背景略亮于画布，边框为 `Divider`，不使用发光阴影。下载主动作可以使用右下角 `Rose` 悬浮按钮或底部胶囊按钮，但同一页面只保留一个。
- **配置/设置页**：将下载目录、语言、主题和工具状态组织成少量配置卡或整行设置项；可编辑对象使用浅灰粉填充和浅色边框，右侧放更多菜单，不使用复杂开关墙。
- **工具页**：使用分组标题和全宽列表行。每行由左侧图标、标题和一行说明组成，行之间使用细分隔线；页面可以自然滚动，不能因为卡片圆角导致视觉碎片化。
- **底部导航**：保留三个功能入口，图标上、文字下；选中项显示 `Rose` 低对比度胶囊，未选中项不显示底板。视频下载器仍使用 `新建`、`队列`、`设置` 等产品语义，不照搬参考应用的“仪表盘/配置/工具”文案。
- **响应式**：宽度 ≥ 420 dp 时仪表盘允许两列统计面板；低于 420 dp 时全部单列。工具列表始终单列，配置卡在窄屏下自动变为整行。
- **状态**：正常/完成使用绿色，进行中使用 `Rose`，警告和失败使用低饱和语义色，并始终配合文字；不依赖颜色单独传达状态。

### 6.2 移动端页面流程

```text
新建
  ↓ 解析媒体
媒体预览
  ↓ 选择格式 / 清晰度
确认下载
  ↓
下载详情
```

媒体预览页：

- 顶部返回按钮和页面标题 `媒体预览`。
- 缩略图位于标题之前，比例 16:9。
- 标题、作者、时长按顺序排列。
- `输出格式` 和 `清晰度` 使用可点击的整行选择器。
- 底部固定主按钮：`下载 MP4` / `下载 MP3`。

下载详情页：

- 顶部显示任务标题和状态。
- 中间只保留一条明显 Signal Trace 和百分比。
- 速度、剩余时间和输出文件名作为次级信息。
- 下载完成后主按钮变为 `打开文件`，次要动作是 `打开下载目录`。

### 6.3 移动端队列页面

队列页面使用分组而不是筛选控件：

```text
下载队列                         [⋯]

正在下载
┌──────────────────────────────┐
│ ● example-video              │
│   MP4 · 43%                  │
│   ━━━━━━━━━╸                 │
│   2.4 MB/s             [取消]│
└──────────────────────────────┘

等待中
another-video       MP3       ›

已完成
finished-video      MP4       ›
```

## 7. 共享组件清单

建议把现有单体 `Main.qml` 拆分为以下组件，桌面端和移动端只切换壳与布局，不复制组件逻辑：

| 组件 | 职责 | 桌面端 | 移动端 |
| --- | --- | --- | --- |
| `AppShell.qml` | 根窗口、主题、全局状态 | 侧边栏 + 活动栏 | 顶栏 + 底部导航 |
| `NavigationRail.qml` | 主导航 | 固定左侧 | 不使用 |
| `MobileBottomBar.qml` | 主导航 | 不使用 | 固定底部 |
| `LinkCapture.qml` | URL 输入、粘贴、拖拽 | 多行输入 + 快捷键 | 多行输入 + 全宽按钮 |
| `MediaPreview.qml` | 缩略图、标题、格式选择 | 横向布局 | 纵向布局 |
| `FormatPicker.qml` | MP4/MP3、清晰度 | 内联选择器 | 底部抽屉 |
| `DownloadRow.qml` | 单个任务和 Signal Trace | 紧凑任务行 | 单列任务项 |
| `QueueView.qml` | 任务分组和空状态 | 中心页或右栏 | 独立页面 |
| `ToolStatusRow.qml` | 工具版本和状态 | 设置页 | 设置页 |
| `InlineNotice.qml` | 成功、警告、错误提示 | 页面内联 | 页面内联 |
| `DropOverlay.qml` | 桌面拖拽反馈 | 启用 | 不启用 |

建议目录：

```text
qml/
├── Main.qml
├── components/
│   ├── AppShell.qml
│   ├── NavigationRail.qml
│   ├── MobileBottomBar.qml
│   ├── LinkCapture.qml
│   ├── MediaPreview.qml
│   ├── FormatPicker.qml
│   ├── DownloadRow.qml
│   ├── QueueView.qml
│   ├── ToolStatusRow.qml
│   ├── InlineNotice.qml
│   └── DropOverlay.qml
├── pages/
│   ├── NewDownloadPage.qml
│   ├── QueuePage.qml
│   ├── SettingsPage.qml
│   └── ToolDiagnosticsPage.qml
├── responsive/
│   ├── Breakpoints.qml
│   └── DeviceMetrics.qml
└── theme/
    ├── Theme.qml
    ├── Typography.qml
    └── Motion.qml
```

## 8. 交互状态和文案

### 8.1 统一状态

| 状态 | 主色 | 标题 | 主操作 |
| --- | --- | --- | --- |
| 空闲 | `Ink` | `等待一个媒体链接` | `粘贴链接` |
| 解析中 | `Violet` | `正在读取媒体信息` | `取消` |
| 可下载 | `Signal` | `媒体已准备好` | `下载 MP4` / `下载 MP3` |
| 等待中 | `Amber` | `等待下载` | `取消等待` |
| 下载中 | `Violet` | `正在下载` | `取消` |
| 已完成 | `Signal` | `下载完成` | `打开文件` |
| 已失败 | `Coral` | `下载未完成` | `重试` |
| 已取消 | `Amber` | `已取消` | `重新下载` |

### 8.2 空状态

队列为空时不要只显示“暂无数据”：

```text
还没有下载任务
粘贴一个公开媒体链接，确认格式后开始下载。

[新建下载]
```

工具缺失时：

```text
下载工具还没有准备好
Video Downloader 需要 yt-dlp 和 FFmpeg 才能解析和处理媒体。

[打开工具诊断]
```

### 8.3 错误原则

- 先说发生了什么，再说用户可以做什么。
- 不使用“出错了”“未知错误”作为唯一信息。
- 错误操作按钮使用动词：`重新解析`、`重试`、`打开工具诊断`。
- 任务失败时保留原始任务信息，避免用户需要重新粘贴链接。

## 9. 动效和反馈

动效只回答用户的动作，不做持续装饰：

- 解析开始：输入区底部 Signal Trace 从左向右绘制一次。
- 解析成功：媒体预览从输入区下方展开，持续 180 ms。
- 进入下载：任务行插入队列，Signal Trace 继续沿用同一位置。
- 完成：进度轨迹转为 `Signal`，显示一次轻微的末端确认动画。
- 失败：轨迹停住并转为 `Coral`，不抖动整张卡片。
- 切换页面：使用 120–180 ms 的横向页面过渡；避免每个列表项同时淡入。
- 用户启用“减少动效”时，保留状态颜色和布局变化，取消位移动画。

## 10. 可访问性

- 所有主按钮、图标按钮和状态图标必须有 `Accessible.name`。
- 键盘焦点使用 2 px 的 `Violet` 外轮廓，不只依赖颜色变化。
- 文字与背景达到可读对比度；状态不能只通过红/绿颜色区分。
- 移动端触摸区域不低于 44 dp，桌面端使用合理的鼠标悬停和键盘操作反馈。
- URL、路径和错误信息允许复制。
- 进度变化通过可访问文本同步，例如 `下载进度 43%`，不只更新视觉进度条。
- 列表项的标题、状态、进度和可用操作按照固定顺序读出。

## 11. QML 实施映射

### 11.1 根窗口

`Main.qml` 和 `MainKirigami.qml` 当前负责：

- 创建 `ApplicationWindow` 或 `Kirigami.ApplicationWindow`。
- 注入 C++ 控制器和模型。
- 根据 Kirigami 是否可用选择应用壳。
- 根据窗口宽度选择桌面双栏或窄屏单列布局。
- 设置主题和页面导航。

下载解析、任务状态、路径检测和文件操作继续留在 C++，不移动到 QML。

### 11.2 响应式断点

```qml
readonly property bool compactWidth: width < 600
readonly property bool mediumWidth: width >= 600 && width < 960
readonly property bool wideWidth: width >= 960
```

建议优先使用布局能力而不是平台名称判断：

- `wideWidth`：桌面工作台布局。
- `mediumWidth`：平板/窄桌面双栏布局。
- `compactWidth`：移动端单栏页面栈布局。

只有在文件选择、窗口行为、路径权限等平台能力不同的时候，才使用平台适配器。

### 11.3 主题实现

现有 `Theme.qml` 应扩展为设计令牌单例，至少包含：

```text
Theme.background
Theme.surface
Theme.surfaceMuted
Theme.text
Theme.muted
Theme.divider
Theme.violet
Theme.signal
Theme.coral
Theme.amber
Theme.radiusInput
Theme.radiusControl
Theme.touchTarget
```

组件不直接写颜色字面量；所有状态颜色、边框、间距和字体通过 `Theme` 或专用令牌对象读取。

### 11.4 Kirigami 页面分层

桌面端 Kirigami 页面位于 `qml/kirigami/`，只在 CMake 检测到 `KF6Kirigami` 时加入 QML 模块；共享的业务模型和基础控件仍位于 `qml/components/` 与 C++ 后端。这样可以让 Kirigami 负责自适应页面结构和桌面交互，同时让 Android 使用同一套下载逻辑和 Qt Quick Controls 2 回退页面。

| 桌面页面 | 主要 Kirigami 组件 | 设计职责 |
| --- | --- | --- |
| `KirigamiNewDownloadPage` | `ScrollablePage`、`Card`、`InlineMessage`、`ActionToolBar` | 链接输入、工具提示、解析状态和媒体预览 |
| `KirigamiQueuePage` | `ScrollablePage`、`Card`、`InlineMessage`、`PlaceholderMessage`、`ActionToolBar` | 队列操作、任务列表和空状态 |
| `KirigamiSettingsPage` | `ScrollablePage`、`Card`、`InlineMessage`、`FormLayout` | 下载目录、工具诊断和默认行为表单 |

Kirigami 卡片只用于有明确操作边界的工作区，任务详情仍复用共享的 `DownloadRow`；状态提示使用 `InlineMessage`，避免再用一组外观相同的自绘提示框。Android 端是否采用 Kirigami，需要单独完成移动端依赖分发和性能验证后再决定。

#### 自适应导航的下一步

- 宽桌面（`>= 1060 px`）继续使用 `GlobalDrawer`，保留工作台和右侧活动栏的横向空间。
- 窄桌面（`560–1059 px`）使用 Kirigami `NavigationTabBar` 放置在窗口底部，将三个一级页面变成容易点击的横向导航；右侧活动栏自动隐藏。
- Android 不直接复用这一实现，继续使用已验收的 Qt Quick Controls 2 `MobileBottomBar`，避免把桌面 Kirigami 运行时带入 APK。
- 导航选中态根据平台使用浅绿底（Windows）或灰粉胶囊（Android），未选中项保持低对比度，页面内容不因为导航切换而复制业务状态。

## 12. 实施状态与下一步

### 已完成

1. [x] 扩展 `Theme.qml`，完成浅色/深色令牌和字体层级。
2. [x] 将主界面拆分为 `NewDownloadPage`、`QueuePage` 和 `SettingsPage`。
3. [x] 实现共享组件：`LinkCapture`、`MediaPreview`、`DownloadRow`、`InlineNotice`。
4. [x] 实现桌面侧栏、活动栏、移动端底部导航和单列布局基础。
5. [x] 接入 Kirigami 页面壳、抽屉和响应式布局。
6. [x] 将真实下载模型、工具状态和任务队列接入页面。
7. [x] 通过 Windows Kirigami Release 构建和 CTest 6/6 验证。
8. [x] 将桌面三类页面切换为 Kirigami 专用实现，并保留 Qt Quick Controls 2 的 Android 回退页面。
9. [x] 通过 `ScrollablePage`、`Card`、`InlineMessage`、`ActionToolBar`、`PlaceholderMessage` 和 `FormLayout` 完成第一轮第三方组件替换。

### 待完成

1. [x] 按 Graphite Rose 参考基线重做 `Theme.qml`、应用壳、底部导航、面板和列表行；首轮实现已在 Android 真机检查。
2. [x] 将桌面页面中的通用卡片、提示、表单和空状态替换为 Kirigami 组件；Android 端保持 Qt Quick Controls 2 回退实现。
3. [x] 在窄桌面窗口实现 Kirigami `NavigationTabBar` 导航；宽桌面保留 `GlobalDrawer`，Android 保留 Qt Quick Controls 2 底部导航。
4. [ ] 将 QML 中的中文界面字符串提取为 Qt 翻译资源，并增加 English 界面。
5. [ ] 检查英文文案在移动端窄屏下的长度、换行和按钮溢出。
6. [ ] 完成键盘焦点、屏幕阅读器和触摸操作的可访问性审查。
7. [ ] 使用 Android/iOS 工具链验证最小可运行目标。
8. [ ] 在真实移动设备上验证 URL 粘贴、下载目录、后台任务和文件分享。

## 13. 设计自检

本方案刻意避免以下容易模板化的做法：

- 不把所有内容做成同样的圆角卡片。
- 不用渐变作为主要视觉识别。
- 不在每个标题上方添加无意义的小标签。
- 不用颜色代替文字状态。
- 不把桌面三栏简单压缩到手机屏幕，而是重组为页面栈和底部导航。
- 移动端默认采用 Graphite Rose 深色基线：深色画布、灰粉主动作、绿色健康状态。
- 选中态使用低对比度胶囊，工具页使用分隔列表，避免“所有内容都是卡片”。

需要保留的核心识别点只有一个：**Signal Trace**。它同时承担品牌、进度和状态反馈，能让界面在浅色、深色、桌面和移动端保持一致。
