# ReClipQt UI 设计方案

> 版本：0.1（设计稿）
> 更新日期：2026-09-05
> 适用范围：Qt 6 + QML/Qt Quick Controls 2 + Kirigami
> 目标平台：Windows、macOS、Linux、Android；iOS 先做布局和构建可行性验证

当前落地状态：视觉令牌、桌面工作台、移动端单列布局、Signal Trace、共享组件和三类页面已经使用 Qt Quick Controls 2 完成。项目已固定并接入 Kirigami 6.8.0（ECM 6.8.0），检测到依赖时使用 `MainKirigami.qml` 的 Kirigami 应用壳；未安装依赖时仍回退到 `Main.qml` 的自定义响应式壳层。两种壳层共享下载后端、主题和页面组件。

## 1. 产品定位

ReClipQt 是一个面向普通用户的媒体下载工作台。它的核心任务不是展示很多设置，而是让用户快速完成：

```text
粘贴链接 → 识别媒体 → 选择格式 → 开始下载 → 查看结果
```

用户应该始终知道三件事：

1. 当前正在处理哪个媒体。
2. 下载进行到了哪里。
3. 下一步可以做什么。

### 1.1 设计关键词

- 清晰：首屏只强调“粘贴链接”和“开始下载”。
- 稳定：任务状态、失败原因和可执行操作必须明确。
- 有信号感：解析和下载过程像一条可靠的媒体信号链，而不是跳动的装饰动画。
- 跨设备：桌面端适合鼠标、键盘和多任务；移动端适合拇指、单列和页面返回。
- 克制：不使用大面积渐变、无意义的卡片堆叠或装饰性数据。

## 2. 视觉方向：Signal Desk

“Signal Desk”把 ReClipQt 设计成一个安静的媒体控制台：深色墨蓝作为工作台背景，紫色表示用户发起的动作，青绿色表示信号已经稳定到达，珊瑚色只用于失败和需要注意的状态。

唯一需要被记住的视觉元素是 **Signal Trace（信号轨迹）**：

- 首次解析时，输入区底部出现一条从紫色向右推进的细线。
- 下载过程中，进度条使用同一条轨迹，不额外添加闪烁、粒子或渐变。
- 完成后轨迹变为青绿色，并在末端显示一个静态完成标记。
- 失败时轨迹停在失败位置，转为珊瑚色，同时显示具体原因和下一步操作。

这样，解析、下载、完成和失败使用同一个视觉语法，用户不需要重新学习状态。

## 3. 设计令牌

### 3.1 核心色板

| 令牌 | Hex | 用途 |
| --- | --- | --- |
| `Ink` | `#101726` | 主文字、深色工作台、深色导航 |
| `Canvas` | `#F4F6FA` | 浅色页面背景 |
| `Violet` | `#6C5CE7` | 主操作、选中态、解析中 |
| `Signal` | `#16B8A6` | 成功、完成、工具正常 |
| `Coral` | `#E67863` | 错误、取消、需要处理 |
| `Amber` | `#E9B95A` | 警告、等待、环境提示 |

派生表面色：

```text
浅色 surface       #FFFFFF
浅色 surface-muted #E9EDF4
浅色 divider       #D8DEE8
深色 canvas        #0B111D
深色 surface       #151D2C
深色 surface-muted #202B3D
深色 divider       #2D3A4F
```

颜色使用规则：

- `Violet` 每个页面最多有一个主要视觉焦点。
- `Signal` 不用作装饰，只表示“已经可用”或“已完成”。
- `Coral` 不作为默认按钮颜色，只用于错误、危险操作和明确的取消状态。
- 页面正文优先使用墨蓝/白色，避免整页使用高饱和色。
- 深色模式不是把浅色模式反转，而是使用深墨蓝工作台和较低亮度表面。

### 3.2 字体

优先使用随应用提供的字体，确保中英文和 Android 设备上的字形一致：

- 主字体：`Noto Sans`，中文回退到 `Noto Sans SC`。
- 技术信息：`IBM Plex Mono`，只用于 URL、文件路径、版本号和进度数字。
- 不使用全大写标签，不使用大面积字母间距。

| 语义 | 桌面端 | 移动端 | 字重 |
| --- | ---: | ---: | --- |
| 页面标题 | 28 px | 24 px | 700 |
| 区块标题 | 18 px | 17 px | 600 |
| 正文 | 14 px | 16 px | 400 |
| 按钮 | 14 px | 15 px | 600 |
| 辅助信息 | 12 px | 13 px | 400 |
| 技术信息 | 12 px | 12 px | 400，等宽 |

### 3.3 间距、尺寸和形状

```text
基础间距：4 / 8 / 12 / 16 / 24 / 32 px
桌面内容最大宽度：1180 px
桌面侧边栏：224 px
桌面右侧活动栏：320 px
移动端页面水平边距：16 px
桌面端最小按钮高度：36 px
移动端最小触摸区域：44 × 44 dp
输入框圆角：12 px
普通控件圆角：8 px
状态标签圆角：999 px
大型页面区块：使用分隔线和留白，不默认套圆角卡片
```

阴影只用于浮层、菜单和底部抽屉。普通内容通过背景层级、分隔线和留白区分，不给每一行都加阴影。

## 4. 信息架构

### 4.1 共享页面

```text
新建下载
├── 链接输入
├── 媒体预览
├── 格式和清晰度选择
└── 开始下载 / 加入队列

下载队列
├── 当前任务
├── 等待中的任务
├── 已完成任务
└── 失败任务

设置
├── 下载目录
├── yt-dlp / FFmpeg
├── 默认输出格式
├── 界面语言
└── 界面主题

工具诊断
├── yt-dlp 状态
├── FFmpeg 状态
└── 重新检测和路径配置
```

### 4.2 导航原则

- 桌面端将“新建下载”“下载队列”“设置”放在左侧导航；“工具诊断”放入设置的子页面，也可以在工具异常时作为明显入口出现。
- 移动端使用底部三项导航：`新建`、`队列`、`设置`。工具诊断作为设置中的页面，不占用底部导航位置。
- 从预览页进入下载详情时使用页面栈，不弹出深层嵌套对话框。
- 选择格式、清晰度等短决策使用底部抽屉或内联展开，不使用多层模态窗口。

## 5. 桌面端设计

### 5.1 桌面端整体布局

适用宽度：`≥ 960 px`。核心目标是同时看见输入、媒体信息和任务活动。

```text
┌────────────────┬─────────────────────────────────────────┬────────────────────┐
│ ReClipQt       │ 新建下载                                │ 活动                │
│                │ 粘贴媒体链接，几秒内开始                 │ 当前任务            │
│  + 新建下载    │                                         │ ────────────────── │
│  下载队列      │ ┌─────────────────────────────────────┐ │ 解析中              │
│                │ │ 粘贴一个或多个媒体链接         ⌘↵ │ │ example.mp4         │
│  设置          │ │                                     │ │ 43% · 2.4 MB/s      │
│                │ │ https://…                           │ │ [取消]              │
│                │ └─────────────────────────────────────┘ │                    │
│                │ [解析媒体]             [清空]            │ 已完成              │
│                │                                         │ clip-01.mp4         │
│  工具状态      │ ───── Signal Trace ─────────────────── │ [打开文件]          │
│  ● 已就绪      │ 媒体预览                                │                    │
│                │ ┌──────────────┐  标题                 │                    │
│  v0.1          │ │              │  作者 / 时长          │                    │
│                │ │   缩略图     │  [MP4] [MP3]           │                    │
│                │ └──────────────┘  [开始下载]            │                    │
└────────────────┴─────────────────────────────────────────┴────────────────────┘
```

布局说明：

- 左侧导航固定，宽度不随内容变化。
- 中间工作区是视觉主角，内容最大宽度约 760 px，左对齐，不居中堆叠。
- 右侧活动栏显示当前任务和最近完成任务，不重复完整下载卡片。
- 输入区是页面唯一的大型强调表面，媒体预览和队列使用轻分隔结构。
- 窗口宽度不足 1180 px 时，右侧活动栏折叠为顶部 `活动`按钮或抽屉。

### 5.2 桌面端“新建下载”页面

首屏只保留一个主要动作：解析链接。

#### 输入状态

- 标题：`新建下载`
- 辅助文案：`粘贴链接，先确认媒体信息，再选择输出格式。`
- 多行输入框支持空格、逗号和换行分隔。
- 主按钮：`解析媒体`
- 次要动作：`从剪贴板粘贴`、`清空`
- 拖拽进入窗口时，整个工作区显示半透明紫色边框，文案为：`松开以解析链接`。

#### 解析成功状态

输入区下方出现 Signal Trace，随后展开媒体预览：

- 缩略图比例 16:9，桌面端宽度约 280 px。
- 标题最多显示两行，作者和时长作为次级信息。
- 输出格式使用两个明确的选择项：`MP4 视频`、`MP3 音频`。
- 清晰度选择显示质量、分辨率和文件大小，不只显示内部 format id。
- 主按钮根据选择变化：`下载 MP4` 或 `下载 MP3`。
- 次要按钮：`加入队列`。

#### 解析失败状态

错误区不使用模糊的“操作失败”。采用：

```text
无法读取这个链接
yt-dlp 没有返回可用的媒体信息。
检查链接是否公开、工具是否可用，然后重试。

[重新解析]  [打开工具诊断]
```

### 5.3 桌面端队列页面

队列使用“任务行 + 状态轨迹”，而不是多个大小相同的卡片：

```text
下载队列                                      [清空已完成]

正在下载                                      1 个任务
┌─ 01 ─────────────────────────────────────────────────────────────┐
│ ● example-video                                                   │
│   MP4 · 1080p · 43%                         2.4 MB/s · 00:18       │
│   ━━━━━━━━━━━━━━━━╸                                             │
│                                  [取消] [打开所在目录]             │
└───────────────────────────────────────────────────────────────────┘

等待中                                        2 个任务
02  another-video              MP3 · 等待中                         [取消]
03  tutorial                   MP4 · 等待中                         [取消]

已完成                                        今天
04  finished-video             MP4 · 182 MB                         [打开文件]
```

状态颜色只出现在任务编号、Signal Trace 和操作按钮上，标题本身保持可读性。

### 5.4 桌面端设置和工具诊断

- 设置页使用两列：左侧设置分类，右侧当前表单；窄窗口时变为页面栈。
- 路径字段使用等宽字体，并支持中间省略。
- 工具状态用一行“状态 + 版本 + 路径 + 操作”，避免三层嵌套卡片。
- `yt-dlp` 和 `FFmpeg` 的状态颜色要同时配合文字：`已就绪`、`未找到`、`版本检测失败`。
- 危险操作“恢复默认设置”放在页面底部，用低强调按钮，不和保存按钮并列。

## 6. 移动端设计

### 6.1 移动端整体布局

适用宽度：`< 600 px`。移动端不是把桌面端缩小，而是把一个下载任务拆成连续、可返回的步骤。

```text
┌────────────────────────────┐
│ ReClipQt              ⋯    │  顶部应用栏
├────────────────────────────┤
│ 新建下载                    │
│ 粘贴链接，马上开始          │
│                            │
│ ┌────────────────────────┐ │
│ │ 粘贴媒体链接           │ │
│ │                        │ │
│ └────────────────────────┘ │
│ [从剪贴板粘贴]             │
│                            │
│ ┌────────────────────────┐ │
│ │ 解析媒体               │ │  主动作
│ └────────────────────────┘ │
│                            │
│ 最近任务                   │
│ 01  example-video   43%   │
│                            │
├────────────────────────────┤
│  新建          队列    设置 │  底部导航
└────────────────────────────┘
```

移动端规则：

- 页面水平边距 16 px，主要按钮全宽。
- 所有可点击区域至少 44 dp 高。
- 不在首屏同时展示工具路径、清晰度策略等高级设置。
- 预览结果使用全宽缩略图，格式和清晰度通过底部抽屉选择。
- 下载任务用单列列表，左侧状态点 + 标题，右侧进度或动作。
- 任务操作使用左滑操作或详情页，不在一行放置三个小按钮。

### 6.2 移动端页面流程

```text
新建
  ↓ 解析媒体
媒体预览
  ↓ 选择格式 / 清晰度
确认下载
  ↓
下载详情
```

媒体预览页：

- 顶部返回按钮和页面标题 `媒体预览`。
- 缩略图位于标题之前，比例 16:9。
- 标题、作者、时长按顺序排列。
- `输出格式` 和 `清晰度` 使用可点击的整行选择器。
- 底部固定主按钮：`下载 MP4` / `下载 MP3`。

下载详情页：

- 顶部显示任务标题和状态。
- 中间只保留一条明显 Signal Trace 和百分比。
- 速度、剩余时间和输出文件名作为次级信息。
- 下载完成后主按钮变为 `打开文件`，次要动作是 `打开下载目录`。

### 6.3 移动端队列页面

队列页面使用分组而不是筛选控件：

```text
下载队列                         [⋯]

正在下载
┌──────────────────────────────┐
│ ● example-video              │
│   MP4 · 43%                  │
│   ━━━━━━━━━╸                 │
│   2.4 MB/s             [取消]│
└──────────────────────────────┘

等待中
another-video       MP3       ›

已完成
finished-video      MP4       ›
```

## 7. 共享组件清单

建议把现有单体 `Main.qml` 拆分为以下组件，桌面端和移动端只切换壳与布局，不复制组件逻辑：

| 组件 | 职责 | 桌面端 | 移动端 |
| --- | --- | --- | --- |
| `AppShell.qml` | 根窗口、主题、全局状态 | 侧边栏 + 活动栏 | 顶栏 + 底部导航 |
| `NavigationRail.qml` | 主导航 | 固定左侧 | 不使用 |
| `MobileBottomBar.qml` | 主导航 | 不使用 | 固定底部 |
| `LinkCapture.qml` | URL 输入、粘贴、拖拽 | 多行输入 + 快捷键 | 多行输入 + 全宽按钮 |
| `MediaPreview.qml` | 缩略图、标题、格式选择 | 横向布局 | 纵向布局 |
| `FormatPicker.qml` | MP4/MP3、清晰度 | 内联选择器 | 底部抽屉 |
| `DownloadRow.qml` | 单个任务和 Signal Trace | 紧凑任务行 | 单列任务项 |
| `QueueView.qml` | 任务分组和空状态 | 中心页或右栏 | 独立页面 |
| `ToolStatusRow.qml` | 工具版本和状态 | 设置页 | 设置页 |
| `InlineNotice.qml` | 成功、警告、错误提示 | 页面内联 | 页面内联 |
| `DropOverlay.qml` | 桌面拖拽反馈 | 启用 | 不启用 |

建议目录：

```text
qml/
├── Main.qml
├── components/
│   ├── AppShell.qml
│   ├── NavigationRail.qml
│   ├── MobileBottomBar.qml
│   ├── LinkCapture.qml
│   ├── MediaPreview.qml
│   ├── FormatPicker.qml
│   ├── DownloadRow.qml
│   ├── QueueView.qml
│   ├── ToolStatusRow.qml
│   ├── InlineNotice.qml
│   └── DropOverlay.qml
├── pages/
│   ├── NewDownloadPage.qml
│   ├── QueuePage.qml
│   ├── SettingsPage.qml
│   └── ToolDiagnosticsPage.qml
├── responsive/
│   ├── Breakpoints.qml
│   └── DeviceMetrics.qml
└── theme/
    ├── Theme.qml
    ├── Typography.qml
    └── Motion.qml
```

## 8. 交互状态和文案

### 8.1 统一状态

| 状态 | 主色 | 标题 | 主操作 |
| --- | --- | --- | --- |
| 空闲 | `Ink` | `等待一个媒体链接` | `粘贴链接` |
| 解析中 | `Violet` | `正在读取媒体信息` | `取消` |
| 可下载 | `Signal` | `媒体已准备好` | `下载 MP4` / `下载 MP3` |
| 等待中 | `Amber` | `等待下载` | `取消等待` |
| 下载中 | `Violet` | `正在下载` | `取消` |
| 已完成 | `Signal` | `下载完成` | `打开文件` |
| 已失败 | `Coral` | `下载未完成` | `重试` |
| 已取消 | `Amber` | `已取消` | `重新下载` |

### 8.2 空状态

队列为空时不要只显示“暂无数据”：

```text
还没有下载任务
粘贴一个公开媒体链接，确认格式后开始下载。

[新建下载]
```

工具缺失时：

```text
下载工具还没有准备好
ReClipQt 需要 yt-dlp 和 FFmpeg 才能解析和处理媒体。

[打开工具诊断]
```

### 8.3 错误原则

- 先说发生了什么，再说用户可以做什么。
- 不使用“出错了”“未知错误”作为唯一信息。
- 错误操作按钮使用动词：`重新解析`、`重试`、`打开工具诊断`。
- 任务失败时保留原始任务信息，避免用户需要重新粘贴链接。

## 9. 动效和反馈

动效只回答用户的动作，不做持续装饰：

- 解析开始：输入区底部 Signal Trace 从左向右绘制一次。
- 解析成功：媒体预览从输入区下方展开，持续 180 ms。
- 进入下载：任务行插入队列，Signal Trace 继续沿用同一位置。
- 完成：进度轨迹转为 `Signal`，显示一次轻微的末端确认动画。
- 失败：轨迹停住并转为 `Coral`，不抖动整张卡片。
- 切换页面：使用 120–180 ms 的横向页面过渡；避免每个列表项同时淡入。
- 用户启用“减少动效”时，保留状态颜色和布局变化，取消位移动画。

## 10. 可访问性

- 所有主按钮、图标按钮和状态图标必须有 `Accessible.name`。
- 键盘焦点使用 2 px 的 `Violet` 外轮廓，不只依赖颜色变化。
- 文字与背景达到可读对比度；状态不能只通过红/绿颜色区分。
- 移动端触摸区域不低于 44 dp，桌面端使用合理的鼠标悬停和键盘操作反馈。
- URL、路径和错误信息允许复制。
- 进度变化通过可访问文本同步，例如 `下载进度 43%`，不只更新视觉进度条。
- 列表项的标题、状态、进度和可用操作按照固定顺序读出。

## 11. QML 实施映射

### 11.1 根窗口

`Main.qml` 只负责：

- 创建 `ApplicationWindow` 或 `Kirigami.ApplicationWindow`。
- 注入 C++ 控制器和模型。
- 选择桌面壳或移动端壳。
- 设置主题、语言和全局快捷键。

下载解析、任务状态、路径检测和文件操作继续留在 C++，不移动到 QML。

### 11.2 响应式断点

```qml
readonly property bool compactWidth: width < 600
readonly property bool mediumWidth: width >= 600 && width < 960
readonly property bool wideWidth: width >= 960
```

建议优先使用布局能力而不是平台名称判断：

- `wideWidth`：桌面工作台布局。
- `mediumWidth`：平板/窄桌面双栏布局。
- `compactWidth`：移动端单栏页面栈布局。

只有在文件选择、窗口行为、路径权限等平台能力不同的时候，才使用平台适配器。

### 11.3 主题实现

现有 `Theme.qml` 应扩展为设计令牌单例，至少包含：

```text
Theme.background
Theme.surface
Theme.surfaceMuted
Theme.text
Theme.muted
Theme.divider
Theme.violet
Theme.signal
Theme.coral
Theme.amber
Theme.radiusInput
Theme.radiusControl
Theme.touchTarget
```

组件不直接写颜色字面量；所有状态颜色、边框、间距和字体通过 `Theme` 或专用令牌对象读取。

## 12. 实施顺序

1. 扩展 `Theme.qml`，完成浅色/深色令牌和字体层级。
2. 将 `Main.qml` 中的页面拆分为 `NewDownloadPage`、`QueuePage`、`SettingsPage` 和 `ToolDiagnosticsPage`。
3. 实现共享组件：`LinkCapture`、`MediaPreview`、`DownloadRow`、`InlineNotice`。
4. 实现桌面端 `AppShell`、`NavigationRail` 和活动栏。
5. 接入 Kirigami 页面栈、抽屉和响应式布局。
6. 实现移动端 `AppShell`、顶部栏、底部导航和单列页面。
7. 使用模拟数据验证桌面宽屏、窄窗口、平板和手机竖屏。
8. 再接入真实下载模型和工具状态，避免 UI 设计被后端状态细节牵着走。
9. 运行 `qmllint`、键盘操作测试、触摸操作测试和至少一次 Android 启动验证。

## 13. 设计自检

本方案刻意避免以下容易模板化的做法：

- 不把所有内容做成同样的圆角卡片。
- 不用渐变作为主要视觉识别。
- 不在每个标题上方添加无意义的小标签。
- 不用颜色代替文字状态。
- 不把桌面三栏简单压缩到手机屏幕，而是重组为页面栈和底部导航。

需要保留的核心识别点只有一个：**Signal Trace**。它同时承担品牌、进度和状态反馈，能让界面在浅色、深色、桌面和移动端保持一致。
